[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "rpi-sentry-app"
version = "0.1.0"
description = "RPI Sentry Application"
authors = ["mstream <maciej.laciak@gmail.com>"]

[tool.poetry.scripts]
app = 'rpi_sentry_app:main'
